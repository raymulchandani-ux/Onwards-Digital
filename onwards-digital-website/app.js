'use strict';
const C=MonarchCore,app=document.querySelector('#app'),modal=document.querySelector('#modal');
const icons={crown:'<path d="m3 6 5 5 4-7 4 7 5-5-3 14H6L3 6Z"/>',arrow:'<path d="M5 12h14m-6-6 6 6-6 6"/>',casino:'<rect x="4" y="3" width="16" height="18" rx="3"/><path d="m12 7 4 5-4 5-4-5 4-5Z"/>',box:'<path d="m3 7 9-4 9 4-9 4-9-4Zm0 0v10l9 4 9-4V7M12 11v10"/>',map:'<path d="m3 5 6-2 6 2 6-2v16l-6 2-6-2-6 2V5Zm6-2v16m6-14v16"/>',coin:'<circle cx="12" cy="12" r="9"/><path d="M15 8h-4a2 2 0 0 0 0 4h2a2 2 0 0 1 0 4H9m3-10v2m0 8v2"/>',plus:'<path d="M12 5v14M5 12h14"/>',close:'<path d="m6 6 12 12M18 6 6 18"/>',car:'<path d="m5 7-2 6v5h18v-5l-2-6H5Zm-2 6h18M6 18v3m12-3v3M6 15h2m8 0h2"/>',home:'<path d="m3 10 9-7 9 7v11H3V10Zm6 11v-8h6v8"/>',watch:'<rect x="6" y="6" width="12" height="12" rx="5"/><path d="m8 6 1-5h6l1 5m-8 12 1 5h6l1-5m-4-9v3l2 1"/>',glasses:'<circle cx="6" cy="13" r="4"/><circle cx="18" cy="13" r="4"/><path d="M10 12h4M2 12l2-6m18 6-2-6"/>',check:'<path d="m5 12 4 4L19 6"/>',chevron:'<path d="m9 5 7 7-7 7"/>',back:'<path d="M19 12H5m6 6-6-6 6-6"/>',info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v6m0-11v2"/>',user:'<circle cx="12" cy="8" r="4"/><path d="M4 22v-3a8 8 0 0 1 16 0v3"/>',expand:'<path d="M9 3H3v6m12-6h6v6M3 15v6h6m12-6v6h-6"/>',minus:'<path d="M5 12h14"/>',spark:'<path d="m12 2 3 7 7 3-7 3-3 7-3-7-7-3 7-3 3-7Z"/>'};
const icon=(name,cls='')=>`<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name]||icons.spark}</svg>`;
const fmt=n=>Number(n).toLocaleString('en-US',{maximumFractionDigits:1}),coin=n=>`<span class="money">${icon('coin')}${fmt(n)}</span>`,esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const products=[
{id:'city-hatch',name:'Kanso City',type:'cars',brand:'KAN SO MOTORS',tier:0,price:4500,cell:0,detail:'A compact classic. Your first set of keys.'},
{id:'veloce-gt',name:'Veloce GT',type:'cars',brand:'VELOCE AUTOMOBILI',tier:1,price:75000,cell:1,detail:'A mid-engine thoroughbred in Rosso Corsa.'},
{id:'noir-one',name:'Noir One',type:'cars',brand:'NOIR AUTOMOTIVE',tier:2,price:480000,cell:2,detail:'Carbon fiber. Uncompromising presence.'},
{id:'aviators',name:'The Aviator',type:'style',brand:'SOL EYEWEAR',tier:0,price:800,cell:3,detail:'Gold frames. An everyday signature.'},
{id:'studio',name:'Harbor Studio',type:'properties',brand:'HARBOR LIVING',tier:0,price:12000,cell:4,detail:'A first home, a short walk from the water.'},
{id:'penthouse',name:'Skyline Penthouse',type:'properties',brand:'SKYLINE ESTATES',tier:1,price:160000,cell:5,detail:'Floor-to-ceiling windows above the city.'},
{id:'mansion',name:'The Palisades',type:'properties',brand:'CROWN PROPERTIES',tier:2,price:850000,cell:6,detail:'A hillside estate with an endless ocean view.'},
{id:'onyx-frames',name:'Onyx Frames',type:'style',brand:'SOL EYEWEAR',tier:1,price:4200,cell:7,detail:'Sculpted acetate. Quiet confidence.'},
{id:'steel-sport',name:'Axiom Sport',type:'watches',brand:'AXIOM',tier:0,price:3200,cell:8,detail:'Brushed steel with a midnight dial.'},
{id:'skeleton',name:'Atelier Skeleton',type:'watches',brand:'ATELIER HOROLOGY',tier:1,price:38000,cell:9,detail:'An open-work movement, made to be seen.'},
{id:'rose-tourbillon',name:'Crown Tourbillon',type:'watches',brand:'MAISON CROWN',tier:2,price:240000,cell:10,detail:'Rose gold. A mechanical work of art.'},
{id:'ivory-frames',name:'Ivory Edition',type:'style',brand:'SOL EYEWEAR',tier:2,price:16000,cell:11,detail:'A distinctive silhouette in polished ivory.'}];
const categories={all:'All locations',properties:'Properties',cars:'Motors',watches:'Watches',style:'Eyewear'},typeIcons={properties:'home',cars:'car',watches:'watch',style:'glasses'},tiers=['Essential','Prestige','Iconic'];
const stores=[{id:'harbor',name:'Harbor Living',type:'properties',tier:0,x:24,y:64},{id:'skyline',name:'Skyline Estates',type:'properties',tier:1,x:55,y:43},{id:'crown',name:'Crown Properties',type:'properties',tier:2,x:71,y:20},{id:'kanso',name:'Kanso Motors',type:'cars',tier:0,x:42,y:77},{id:'veloce',name:'Veloce Automobili',type:'cars',tier:1,x:65,y:60},{id:'noir',name:'Noir Automotive',type:'cars',tier:2,x:79,y:38},{id:'axiom',name:'Axiom',type:'watches',tier:0,x:36,y:48},{id:'atelier',name:'Atelier Horology',type:'watches',tier:1,x:50,y:27},{id:'maison',name:'Maison Crown',type:'watches',tier:2,x:83,y:70},{id:'sol',name:'Sol Eyewear',type:'style',tier:null,x:58,y:83}];
const fresh=()=>({version:1,balance:25000,owned:[],equipped:{},name:'Newcomer',rounds:0,totalWon:0,history:[],bj:null,poker:null,roulette:null,joined:false});
let storageOK=true,state;try{state=JSON.parse(localStorage.getItem('monarch-v1'))||fresh();if(state.version!==1||!Number.isFinite(state.balance)||state.balance<0||!Array.isArray(state.owned))state=fresh();state={...fresh(),...state};state.name=typeof state.name==='string'&&state.name.trim()?state.name.slice(0,18):'Newcomer';state.equipped=state.equipped&&typeof state.equipped==='object'?state.equipped:{};state.history=Array.isArray(state.history)?state.history.filter(h=>h&&Number.isFinite(h.net)&&typeof h.game==='string').slice(0,20):[];state.rounds=Number.isInteger(state.rounds)&&state.rounds>=0?state.rounds:0;for(const k of ['bj','poker'])if(state[k]&&(!Array.isArray(state[k].deck)||!['playing','done'].includes(state[k].phase)||!Number.isFinite(state[k].bet)))state[k]=null}catch(e){state=fresh();storageOK=false}
let activeGame='blackjack',mapFilter='all',inventoryFilter='all',storeId=null,mapZoom=1,rouletteChoice='red',bet=100,rouletteBusy=false,toastTimer;
function save(){try{localStorage.setItem('monarch-v1',JSON.stringify(state))}catch(e){storageOK=false}}
function notify(text){const t=document.querySelector('#toast');t.textContent=text;t.classList.add('visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.remove('visible'),3600)}
const brand=()=>`<span class="brand">${icon('crown')}<span>MONARCH<span class="brand-dot">.</span></span></span>`;
function route(){const r=location.hash.slice(1)||'home';return ['home','begin','play','inventory','purchase'].includes(r)?r:'home'}
function go(r){if(route()===r){render();return}location.hash=r}
function header(r){const inside=['play','inventory','purchase'].includes(r);return `<header class="header"><div class="header-inner">${brand()}${inside?`<nav aria-label="Game sections">${[['play','casino','Play'],['inventory','box','Inventory'],['purchase','map','Purchase']].map(([id,ico,label])=>`<a href="#${id}" class="nav-item ${r===id?'active':''}" ${r===id?'aria-current="page"':''}>${icon(ico)}<span>${label}</span></a>`).join('')}</nav>`:`<span class="header-caption">YOUR WORLD. YOUR WAY.</span>`}<div class="header-right">${inside?`<div class="balance"><span class="balance-label">YOUR BALANCE</span>${coin(state.balance)}</div><button class="wallet-add" data-action="wallet" aria-label="Add demo credits">${icon('plus')}</button><button class="avatar-button" data-action="profile" aria-label="Edit player name">${esc(state.name[0].toUpperCase())}</button>`:`<span class="version-label"><i></i> EARLY ACCESS</span>`}</div></div></header>`}
function footer(){return `<footer><span>${brand()}<span class="footer-copy">A little play. A life of possibility.</span></span><span>Virtual credits only <span class="dot">·</span> No cash value <span class="dot">·</span> Local demo</span></footer>`}
function home(){return `<main class="home"><div class="hero-image"></div><div class="hero-content"><div class="eyebrow"><span class="line"></span> WELCOME TO YOUR NEXT CHAPTER</div><h1>Make your<br>own <em>fortune.</em></h1><p>Play your hand. Build your collection.<br>Live a little larger.</p><button class="primary hero-cta" data-action="begin">Enter Monarch ${icon('arrow')}</button><div class="starting">${icon('coin')} Start with <strong>25,000 free credits</strong><span>Yours to play with.</span></div></div><div class="hero-coordinate">MONARCH CITY <span>34°03′ N &nbsp; 118°15′ W</span></div><div class="home-bottom"><span>THE TABLES ARE JUST THE BEGINNING</span><div><span>01 <b>Play</b></span><span>02 <b>Collect</b></span><span>03 <b>Make it yours</b></span></div><span class="scroll-mark">${icon('arrow')}</span></div></main><section class="home-strip"><span>${icon('casino')} Three classic games</span><span>${icon('map')} One city to make your own</span><span>${icon('box')} A collection that tells your story</span><span class="muted">No real-money wagering.</span></section>`}
function begin(){return `<main class="container gateway"><div class="eyebrow">THE CITY IS YOURS</div><h1>Where to first?</h1><p class="muted">A seat at the table. A place in the city. Your next move.</p><div class="gateway-cards">${[['play','casino','01','Take your seat.','Play','Blackjack, poker, and roulette. Make your first move.'],['inventory','box','02','Define your style.','Inventory','Your character, your keys, your growing collection.'],['purchase','map','03','Find your next want.','Purchase','Explore the city. Turn your credits into something yours.']].map(([id,ico,n,title,label,desc])=>`<a href="#${id}" class="gateway-card gateway-${id}"><span class="gateway-number">${n} / MONARCH</span><div class="gateway-art">${icon(ico)}</div><div class="gateway-copy"><span class="eyebrow">${title}</span><h2>${label} ${icon('arrow')}</h2><p>${desc}</p></div></a>`).join('')}</div><div class="gateway-note">${icon('coin')} Your starting balance is ready. ${coin(state.balance)}</div></main>`}
function render(){const previous=document.activeElement;const focusData=previous?.dataset?.action?{...previous.dataset}:null;const r=route();app.innerHTML=header(r)+(r==='home'?home():r==='begin'?begin():r==='play'?play():r==='inventory'?inventory():purchase())+footer();if(r==='play')bindBet();if(focusData){const candidate=Array.from(app.querySelectorAll('[data-action]')).find(el=>Object.entries(focusData).every(([k,v])=>el.dataset[k]===v));if(candidate&&!candidate.disabled)candidate.focus({preventScroll:true});}if(!storageOK&&!document.querySelector('.storage-notice'))app.insertAdjacentHTML('beforeend','<div class="storage-notice">Browser saving is unavailable. Progress lasts for this visit only.</div>')}
app.addEventListener('click',e=>{const el=e.target.closest('[data-action]');if(el)action(el.dataset.action,el.dataset)});
modal.addEventListener('click',e=>{if(e.target===modal)closeModal();const el=e.target.closest('[data-action]');if(el)action(el.dataset.action,el.dataset)});
window.addEventListener('hashchange',()=>{closeModal();render();window.scrollTo(0,0)});
function closeModal(){if(modal.open)modal.close()}
function showModal(title,content){modal.innerHTML=`<div class="modal-head"><h2 id="modal-title">${title}</h2><button class="icon-button" data-action="close" aria-label="Close dialog">${icon('close')}</button></div>${content}`;modal.showModal()}
function action(a,d){if(a==='begin'){state.joined=true;save();go('begin')}else if(a==='wallet')wallet();else if(a==='close')closeModal();else if(a==='profile')profile();else if(a==='save-profile'){const value=modal.querySelector('input').value.trim().slice(0,18);if(!value){notify('Enter a player name');return}state.name=value;save();closeModal();render()}else if(a==='demo-coins'){state.balance+=Number(d.amount);save();closeModal();render();notify(`${fmt(d.amount)} demo credits added. No payment taken.`)}else if(a==='game'){activeGame=d.game;render()}else if(a==='bet-adjust'){const input=document.querySelector('#bet');bet=Math.min(5000,Math.max(10,Math.floor((Number(input.value)||100)*Number(d.factor))));input.value=bet}else if(a==='bj-deal')bjDeal();else if(a==='bj-hit')bjHit();else if(a==='bj-stand')bjStand();else if(a==='bj-double')bjDouble();else if(a==='poker-deal')pokerDeal();else if(a==='poker-hold'){state.poker.held[Number(d.index)]=!state.poker.held[Number(d.index)];save();render()}else if(a==='poker-draw')pokerDraw();else if(a==='roulette-choice'){if(rouletteBusy)return;rouletteChoice=d.choice;render()}else if(a==='roulette-spin')rouletteSpin();else if(a==='rules')rules();else if(a==='map-filter'){mapFilter=d.filter;storeId=null;render()}else if(a==='store'){storeId=d.id;render()}else if(a==='store-close'){storeId=null;render()}else if(a==='zoom'){mapZoom=Math.max(1,Math.min(1.7,mapZoom+Number(d.delta)));render()}else if(a==='reset-map'){mapZoom=1;render()}else if(a==='buy')confirmBuy(d.id);else if(a==='confirm-buy')buy(d.id);else if(a==='inventory-filter'){inventoryFilter=d.filter;render()}else if(a==='equip'){const p=products.find(p=>p.id===d.id);if(!p||!state.owned.includes(p.id))return;state.equipped[p.type]=state.equipped[p.type]===p.id?null:p.id;save();render();notify(state.equipped[p.type]?`${p.name} equipped`:`${p.name} unequipped`)} }
function wallet(){showModal('Your wallet',`<div class="wallet-total"><span class="muted">AVAILABLE CREDITS</span>${coin(state.balance)}</div><p class="muted">Top up your demo balance. These packs are previews: no real payment is collected.</p><div class="coin-packs">${[[25000,'Starter','$4.99'],[100000,'Collector','$14.99'],[500000,'High life','$49.99']].map(([amount,name,price])=>`<button class="coin-pack" data-action="demo-coins" data-amount="${amount}">${icon('coin')}<span>${name}</span><strong>${fmt(amount)}</strong><small>${price} concept price</small><b>Add demo credits ${icon('plus')}</b></button>`).join('')}</div><p class="fine">Credits and items have no cash value and cannot be withdrawn or transferred.</p>`)}
function profile(){showModal('Make a name for yourself',`<label class="field-label" for="player-name">Player name</label><input id="player-name" maxlength="18" value="${esc(state.name)}" autocomplete="nickname"><button class="primary full" data-action="save-profile">Save name ${icon('check')}</button><p class="fine">Your profile is saved in this browser.</p>`)}
render();
function play(){const names={blackjack:'Blackjack',poker:'Poker',roulette:'Roulette'};return `<main class="container casino-page"><div class="page-heading"><div><div class="eyebrow">THE CASINO</div><h1>A good hand. A great night.</h1></div><span class="quiet-tag">${icon('coin')} Play with virtual credits</span></div><div class="game-tabs" role="group" aria-label="Casino games">${Object.entries(names).map(([id,name],i)=>`<button aria-pressed="${activeGame===id}" class="game-tab ${activeGame===id?'selected':''}" data-action="game" data-game="${id}"><span class="game-tab-symbol">${['♠','♦','◉'][i]}</span><span><b>${name}</b><small>${['The classic 21','Five-card video poker','European · single zero'][i]}</small></span><span class="tab-arrow">${icon('arrow')}</span></button>`).join('')}</div><div class="game-shell"><aside class="bet-panel"><div class="panel-label">${icon('casino')} MONARCH ORIGINALS</div><h2>${names[activeGame]}</h2><div class="bet-mode"><span>Manual play</span><span class="status-dot"></span></div><label class="field-label" for="bet">Bet amount <span>Credits</span></label><div class="bet-input">${icon('coin')}<input id="bet" type="number" min="10" max="5000" step="1" value="${bet}" inputmode="numeric" ${isRoundActive()?'disabled':''}></div><div class="bet-shortcuts"><button data-action="bet-adjust" data-factor="0.5" ${isRoundActive()?'disabled':''}>½</button><button data-action="bet-adjust" data-factor="2" ${isRoundActive()?'disabled':''}>2×</button><span>10 – 5,000</span></div>${gameControls()}<div class="game-hint">${activeGame==='blackjack'?'Get closer to 21 than the dealer. Face cards count as 10.':activeGame==='poker'?'Hold the cards you like, then draw once. A pair of jacks or better pays.':'Pick a color, an outside bet, or a single number. Then spin the wheel.'}</div><div class="bet-panel-bottom"><button class="text-button" data-action="rules">${icon('info')} Rules & payouts</button><span>Randomly shuffled · No cash value</span></div></aside><section class="table-wrap" aria-label="${names[activeGame]} table">${activeGame==='blackjack'?blackjackTable():activeGame==='poker'?pokerTable():rouletteTable()}</section></div><div class="session-bar"><div><span class="session-dot"></span> YOUR SESSION <span class="muted">${state.rounds} rounds played</span></div><div><span class="muted">Recent results</span>${state.history.length?state.history.slice(0,5).map(h=>`<span class="history-pill ${h.net>0?'positive':h.net<0?'negative':''}" title="${esc(h.game)}">${h.net>0?'+':''}${fmt(h.net)}</span>`).join(''):'<span class="muted">Your first round is waiting</span>'}</div></div></main>`}
function bindBet(){const input=document.querySelector('#bet');input?.addEventListener('input',()=>{bet=Number(input.value)})}
function isRoundActive(){return activeGame==='blackjack'?state.bj?.phase==='playing':activeGame==='poker'?state.poker?.phase==='playing':rouletteBusy}
function gameControls(){if(activeGame==='blackjack'){const playing=state.bj?.phase==='playing';return playing?`<div class="split-controls"><button class="primary" data-action="bj-hit">Hit ${icon('plus')}</button><button class="secondary" data-action="bj-stand">Stand</button></div><button class="secondary full double-button" data-action="bj-double" ${state.bj.player.length!==2||state.balance<state.bj.bet?'disabled':''}>Double down</button><div class="wager-note">On the table ${coin(state.bj.bet)}</div>`:`<button class="primary full deal-button" data-action="bj-deal">${state.bj?'Deal again':'Deal cards'} ${icon('arrow')}</button>`}if(activeGame==='poker')return `<button class="primary full deal-button" data-action="${state.poker?.phase==='playing'?'poker-draw':'poker-deal'}">${state.poker?.phase==='playing'?'Draw cards':state.poker?'Deal again':'Deal cards'} ${icon('arrow')}</button>${state.poker?.phase==='playing'?`<div class="wager-note">On the table ${coin(state.poker.bet)}</div>`:''}`;return `<div class="roulette-selection"><span class="muted">Your selection</span><b>${choiceName(rouletteChoice)}</b></div><button class="primary full deal-button" data-action="roulette-spin" ${rouletteBusy?'disabled':''}>${rouletteBusy?'Spinning…':'Spin wheel'} ${icon('arrow')}</button>`}
function card(c,{hidden=false,index=0,hold=false,held=false}={}){if(hidden)return `<div class="playing-card card-back" aria-label="Face-down card" style="--i:${index}">${icon('crown')}<span>MONARCH</span></div>`;const rank=c.rank===14?'A':c.rank===13?'K':c.rank===12?'Q':c.rank===11?'J':c.rank,inside=`<span class="card-corner">${rank}<small>${c.suit}</small></span><span class="card-suit">${c.suit}</span><span class="card-corner bottom">${rank}<small>${c.suit}</small></span>`;return hold?`<button class="hold-card ${held?'held':''}" data-action="poker-hold" data-index="${index}" aria-pressed="${held}" aria-label="${held?'Release':'Hold'} ${rank} ${c.suit}"><span class="playing-card ${['♥','♦'].includes(c.suit)?'red-card':''}" style="--i:${index}">${inside}</span><span class="hold-label">${held?'HELD':'HOLD'}</span></button>`:`<div class="playing-card ${['♥','♦'].includes(c.suit)?'red-card':''}" style="--i:${index}" aria-label="${rank} ${c.suit}">${inside}</div>`}
function tableBrand(){return `<div class="table-brand">${icon('crown')} MONARCH <small>THE PRIVATE TABLES</small></div>`}
function resultBanner(game){if(!game||game.phase==='playing')return '';return `<div class="result-banner ${game.result.multiplier>1?'won':game.result.multiplier===0?'lost':''}" role="status"><b>${esc(game.result.label)}</b><span>${game.result.multiplier===0?`−${fmt(game.bet)} credits`:`${fmt(game.bet*game.result.multiplier)} credits returned`}</span></div>`}
function blackjackTable(){const g=state.bj,playing=g?.phase==='playing';return `<div class="table-topline"><span>BLACKJACK PAYS 3:2</span><span>TABLE 01 <i></i></span></div><div class="blackjack-felt"><div class="dealer-position"><span class="hand-label">DEALER ${g?`<b>${playing?C.blackjackScore([g.dealer[0]]):C.blackjackScore(g.dealer)}</b>`:''}</span><div class="hand">${g?g.dealer.map((c,i)=>card(c,{hidden:playing&&i===1,index:i})).join(''):[0,1].map(i=>card(null,{hidden:true,index:i})).join('')}</div></div>${tableBrand()}<div class="table-arc">DEALER STANDS ON ALL 17s <span>•</span> SINGLE DECK</div><div class="player-position"><div class="hand">${g?g.player.map((c,i)=>card(c,{index:i})).join(''):`<div class="empty-card"></div><div class="empty-card"></div>`}</div><span class="hand-label">${g?'YOUR HAND':'YOUR SEAT'} ${g?`<b>${C.blackjackScore(g.player)}</b>`:''}</span></div>${resultBanner(g)}${!g?'<div class="table-welcome">Every story starts with a first hand.</div>':''}</div>`}
function pokerTable(){const g=state.poker;return `<div class="table-topline"><span>JACKS OR BETTER · FIVE-CARD DRAW</span><span>TABLE 02 <i></i></span></div><div class="poker-felt">${tableBrand()}<div class="poker-paytable">${C.paytable.map(([name,m])=>`<div class="${g?.phase==='done'&&g.result.label===name?'paytable-win':''}"><span>${name}</span><b>${m}×</b></div>`).join('')}</div><div class="poker-hand">${g?g.hand.map((c,i)=>card(c,{index:i,hold:g.phase==='playing',held:g.held[i]})).join(''):[0,1,2,3,4].map(i=>card(null,{hidden:true,index:i})).join('')}</div><div class="poker-instruction">${g?.phase==='playing'?'Select cards to hold. Replace the rest with one draw.':'ONE HAND. FIVE CARDS. YOUR CALL.'}</div>${resultBanner(g)}</div>`}
function choiceName(c){return /^n\d+$/.test(c)?`Number ${c.slice(1)}`:({red:'Red',black:'Black',odd:'Odd',even:'Even',low:'1 to 18',high:'19 to 36'}[c]||'Red')}
function rouletteTable(){const g=state.roulette,segments=C.wheel.map((n,i)=>`${n===0?'#438366':C.red.includes(n)?'#a84552':'#16232e'} ${i*360/37}deg ${(i+1)*360/37}deg`).join(',');return `<div class="table-topline"><span>EUROPEAN ROULETTE · 37 POCKETS</span><span>TABLE 03 <i></i></span></div><div class="roulette-felt"><div class="wheel-zone"><div class="wheel-pointer"></div><div class="roulette-wheel ${rouletteBusy?'spinning':''}" style="--wheel-fill:conic-gradient(from -${180/37}deg,${segments});--rotation:${g?.rotation||0}deg;--previous:${g?.previous||0}deg"><div class="wheel-numbers">${C.wheel.map((n,i)=>`<span style="--a:${i*360/37}deg">${n}</span>`).join('')}</div><div class="wheel-hub">${icon('crown')}<small>MONARCH</small></div></div><div class="wheel-result" aria-live="polite">${rouletteBusy?'Spinning…':g?`<b class="pocket ${g.number===0?'green':C.red.includes(g.number)?'red':'black'}">${g.number}</b><span>${g.result.multiplier?'Winning spin':'Spin complete'}</span>`:'<span>YOUR NEXT MOVE AWAITS</span>'}</div></div><div class="roulette-bets"><div class="number-grid"><button class="number-bet zero ${rouletteChoice==='n0'?'chosen':''}" data-action="roulette-choice" data-choice="n0" aria-pressed="${rouletteChoice==='n0'}" ${rouletteBusy?'disabled':''}>0</button>${Array.from({length:36},(_,i)=>i+1).map(n=>`<button class="number-bet ${C.red.includes(n)?'red':'black'} ${rouletteChoice===`n${n}`?'chosen':''}" data-action="roulette-choice" data-choice="n${n}" aria-pressed="${rouletteChoice===`n${n}`}" ${rouletteBusy?'disabled':''}>${n}</button>`).join('')}</div><div class="outside-bets">${['low','even','red','black','odd','high'].map(c=>`<button class="${rouletteChoice===c?'chosen':''} ${c==='red'?'red-outside':''}" data-action="roulette-choice" data-choice="${c}" aria-pressed="${rouletteChoice===c}" ${rouletteBusy?'disabled':''}>${choiceName(c)}</button>`).join('')}</div><div class="roulette-payout-note">Outside bets return 2× <span>Single numbers return 36×</span></div></div>${rouletteBusy?'':resultBanner(g)}</div>`}
function readWager(){const value=Number(document.querySelector('#bet')?.value??bet);if(!C.validBet(value,state.balance)){notify(value>state.balance?'Not enough credits. Use the + button to add demo credits.':'Choose a whole-number bet from 10 to 5,000 credits.');return null}bet=value;return value}
function record(game,wager,result){const amount=wager*result.multiplier;state.balance+=amount;state.rounds++;if(amount>wager)state.totalWon+=amount-wager;state.history.unshift({game,net:amount-wager});state.history=state.history.slice(0,20);save()}
function bjDeal(){if(state.bj?.phase==='playing')return;const wager=readWager();if(wager===null)return;state.balance-=wager;const deck=C.deck();state.bj={deck,player:[deck.pop(),deck.pop()],dealer:[deck.pop(),deck.pop()],bet:wager,phase:'playing'};if(C.blackjackScore(state.bj.player)===21||C.blackjackScore(state.bj.dealer)===21)bjSettle(false);else{save();render()}}
function bjSettle(drawDealer=true){const g=state.bj;if(!g||g.phase!=='playing')return;if(drawDealer&&C.blackjackScore(g.player)<=21)while(C.blackjackScore(g.dealer)<17)g.dealer.push(g.deck.pop());g.result=C.blackjackResult(g.player,g.dealer);g.phase='done';record('Blackjack',g.bet,g.result);render()}
function bjHit(){const g=state.bj;if(!g||g.phase!=='playing')return;g.player.push(g.deck.pop());if(C.blackjackScore(g.player)>=21)bjSettle();else{save();render()}}
function bjStand(){bjSettle()}
function bjDouble(){const g=state.bj;if(!g||g.phase!=='playing'||g.player.length!==2||state.balance<g.bet)return;state.balance-=g.bet;g.bet*=2;g.player.push(g.deck.pop());bjSettle()}
function pokerDeal(){if(state.poker?.phase==='playing')return;const wager=readWager();if(wager===null)return;state.balance-=wager;const deck=C.deck();state.poker={deck,hand:[deck.pop(),deck.pop(),deck.pop(),deck.pop(),deck.pop()],held:[false,false,false,false,false],phase:'playing',bet:wager};save();render()}
function pokerDraw(){const g=state.poker;if(!g||g.phase!=='playing')return;g.hand=g.hand.map((c,i)=>g.held[i]?c:g.deck.pop());g.result=C.pokerResult(g.hand);g.phase='done';record('Poker',g.bet,g.result);render()}
function rouletteSpin(){if(rouletteBusy)return;const wager=readWager();if(wager===null)return;state.balance-=wager;const number=C.randomInt(37),result=C.rouletteResult(number,rouletteChoice),prev=state.roulette?.rotation||0;const rotation=Math.ceil(prev/360)*360+1440+(360-C.wheel.indexOf(number)*360/37)%360;state.roulette={number,result,bet:wager,rotation,previous:prev,phase:'done',choice:rouletteChoice};record('Roulette',wager,result);rouletteBusy=true;render();setTimeout(()=>{rouletteBusy=false;if(route()==='play'&&activeGame==='roulette')render()},window.matchMedia('(prefers-reduced-motion: reduce)').matches?80:2700)}
function rules(){showModal('House rules',`<div class="rules"><h3>Blackjack</h3><p>Beat the dealer without exceeding 21. Aces count as 1 or 11; face cards count as 10. The dealer stands on every 17, including soft 17. A two-card blackjack pays 3:2 profit, a regular win pays 1:1, and a tie returns the bet. Double down is available on your first two cards and gives exactly one more card. No splitting or insurance.</p><h3>Five-card video poker</h3><p>Deal five cards, select any cards to hold, then draw once to replace the rest. This is single-player Jacks or Better video poker. The table shows total returns, including the original bet. A pair of jacks, queens, kings, or aces returns 1×; a royal flush returns 800×. All hands use a fresh 52-card deck.</p><h3>European roulette</h3><p>One bet per spin on a 37-pocket wheel (0–36). Red, black, odd, even, low, and high pay 1:1 profit (2× total return); single-number bets pay 35:1 (36× total return). Zero loses all outside bets. Each pocket is equally likely.</p><h3>Your credits</h3><p>Bets range from 10 to 5,000 credits. There are no withdrawals, cash prizes, or transfers. Progress lives in this browser. An unfinished card hand stays at the table when you navigate away; a roulette spin is settled immediately and its animation reveals the result.</p></div>`)}
function art(p,cls=''){return `<div class="product-art ${cls}" role="img" aria-label="${esc(p.name)}" style="--col:${p.cell%4};--art-ratio:${[362/310,362/374,362/366][Math.floor(p.cell/4)]};--art-y:${[0.6,46.35,98.33][Math.floor(p.cell/4)]}%"></div>`}
function inventory(){const owned=products.filter(p=>state.owned.includes(p.id)),shown=owned.filter(p=>inventoryFilter==='all'||p.type===inventoryFilter),value=owned.reduce((n,p)=>n+p.price,0);return `<main class="container inventory-page"><div class="page-heading"><div><div class="eyebrow">YOUR COLLECTION</div><h1>Success looks good on you.</h1></div><span class="quiet-tag">${icon('box')} ${owned.length} / ${products.length} collected</span></div><div class="inventory-layout"><aside class="profile-card"><div class="profile-scene"><div class="profile-label">MONARCH CITIZEN <span>01</span></div><div class="profile-person"></div><div class="profile-caption"><span class="eyebrow">${value>=200000?'THE HIGH LIFE':value>=20000?'MAKING MOVES':'A FRESH START'}</span><h2>${esc(state.name)}</h2><button class="text-button" data-action="profile">Edit name ${icon('chevron')}</button></div></div><div class="profile-stats"><div><span>Collection value</span>${coin(value)}</div><div><span>Items equipped</span><b>${Object.values(state.equipped).filter(Boolean).length} / 4</b></div></div><div class="loadout"><div class="panel-label">YOUR SIGNATURE</div>${Object.entries(typeIcons).map(([type,ico])=>{const p=products.find(p=>p.id===state.equipped[type]);return `<div class="loadout-row">${icon(ico)}<div><small>${categories[type]}</small><span>${p?p.name:'Nothing equipped'}</span></div>${p?`<span class="equipped-dot"></span>`:''}</div>`}).join('')}</div></aside><section class="collection"><div class="collection-top"><h2>The things you make yours.</h2><p class="muted">Equip your favorites to feature them on your character profile.</p></div><div class="filter-tabs" aria-label="Inventory categories">${Object.entries({...categories,all:'All items'}).map(([id,name])=>`<button class="${inventoryFilter===id?'active':''}" data-action="inventory-filter" data-filter="${id}" aria-pressed="${inventoryFilter===id}">${name}${id==='all'?` <small>${owned.length}</small>`:''}</button>`).join('')}</div>${shown.length?`<div class="inventory-grid">${shown.map(p=>`<article class="item-card"><div class="item-top"><span class="rarity tier-${p.tier}">${tiers[p.tier]}</span><span class="item-type">${icon(typeIcons[p.type])}</span></div>${art(p)}<div class="item-copy"><small>${p.brand}</small><h3>${p.name}</h3><div class="item-value">${coin(p.price)}</div><button class="${state.equipped[p.type]===p.id?'equipped-button':'secondary'} full" data-action="equip" data-id="${p.id}">${state.equipped[p.type]===p.id?`${icon('check')} Equipped · remove`:'Equip item'}</button></div></article>`).join('')}</div>`:`<div class="empty-inventory">${icon(inventoryFilter==='all'?'box':typeIcons[inventoryFilter])}<h3>${owned.length?'Nothing here yet.':'Your story is still unwritten.'}</h3><p>Explore the city through <strong>Purchase</strong> in the header.<br>Your next find will appear right here.</p><span>Start small. Make it personal.</span></div>`}<div class="collection-note">${icon('info')} Your equipped favorites form your signature collection. Change them whenever you like.</div></section></div></main>`}
function purchase(){const visible=stores.filter(s=>mapFilter==='all'||s.type===mapFilter),selected=stores.find(s=>s.id===storeId);return `<main class="container purchase-page"><div class="page-heading"><div><div class="eyebrow">MONARCH CITY</div><h1>A whole new kind of playground.</h1></div><span class="quiet-tag">${icon('map')} 10 destinations. Endless ambition.</span></div><div class="city-toolbar"><div class="filter-tabs" aria-label="Map location categories">${Object.entries(categories).map(([id,name])=>`<button class="${mapFilter===id?'active':''}" data-action="map-filter" data-filter="${id}" aria-pressed="${mapFilter===id}">${icon(id==='all'?'map':typeIcons[id])}${name}</button>`).join('')}</div><span class="city-time">BLUE HOUR <i></i> 19:42</span></div><div class="city-layout"><section class="city-map" aria-label="Interactive Monarch City shopping map"><div class="map-plane" style="--zoom:${mapZoom}"><img src="assets/city.jpg" alt="Bird's-eye view of fictional Monarch City with coastline, marina, downtown and hillside districts" draggable="false"><span class="district district-coast">PACIFIC COAST</span><span class="district district-hills">THE PALISADES</span><span class="district district-downtown">DOWNTOWN</span><span class="district district-marina">SOUTH MARINA</span>${visible.map(s=>`<button class="map-pin pin-${s.type} ${storeId===s.id?'pin-selected':''}" style="left:${s.x}%;top:${s.y}%" data-action="store" data-id="${s.id}" aria-label="Visit ${s.name}, ${s.tier===null?'all tiers':tiers[s.tier]} ${categories[s.type]}"><span>${icon(typeIcons[s.type])}</span><b>${s.name}</b></button>`).join('')}</div><div class="map-top-label">${icon('crown')} MONARCH CITY <span>EXPLORE THE GOOD LIFE</span></div><div class="map-compass"><span>N</span><i></i><small>S</small></div><div class="map-controls"><button data-action="zoom" data-delta="0.2" aria-label="Zoom in">${icon('plus')}</button><button data-action="zoom" data-delta="-0.2" aria-label="Zoom out">${icon('minus')}</button><button data-action="reset-map" aria-label="Reset map zoom">${icon('expand')}</button></div><div class="map-scale">────── <span>500 m · fictional city</span></div><span class="map-hint">Select a location to step inside</span></section><aside class="city-sidebar">${selected?storePanel(selected):`<div class="district-guide"><div class="eyebrow">THE CITY DIRECTORY</div><h2>Find your next<br>favorite thing.</h2><p class="muted">From your first apartment to the keys you've always wanted.</p></div><div class="store-list">${visible.map(s=>`<button class="store-list-item" data-action="store" data-id="${s.id}"><span class="store-icon type-${s.type}">${icon(typeIcons[s.type])}</span><span><b>${s.name}</b><small>${s.tier===null?'Everyday to iconic':tiers[s.tier]} · ${categories[s.type]}</small></span>${icon('chevron')}</button>`).join('')}</div><div class="directory-foot">${icon('spark')} A little taste. A lot of possibility.</div>`}</aside></div><div class="map-legend">${Object.entries(typeIcons).map(([type,ico])=>`<span class="legend-${type}">${icon(ico)}${categories[type]}</span>`).join('')}<span class="muted">Fictional brands. Real main-character energy.</span></div></main>`}
function storePanel(store){const items=products.filter(p=>p.type===store.type&&(store.tier===null||p.tier===store.tier));return `<button class="text-button back-directory" data-action="store-close">${icon('back')} City directory</button><div class="store-heading"><span class="store-icon type-${store.type}">${icon(typeIcons[store.type])}</span><div class="eyebrow">${store.tier===null?'A NEW PERSPECTIVE':tiers[store.tier].toUpperCase()+' COLLECTION'}</div><h2>${store.name}</h2><p class="muted">${{cars:'Your next chapter comes with a set of keys.',properties:'Find a place to call yours.',watches:'Good taste is always on time.',style:'See the city a little differently.'}[store.type]}</p></div><div class="store-products">${items.map(p=>`<article class="shop-item">${art(p)}<span class="rarity tier-${p.tier}">${tiers[p.tier]}</span><h3>${p.name}</h3><p>${p.detail}</p><div class="shop-price">${coin(p.price)}<span>virtual credits</span></div><button class="${state.owned.includes(p.id)?'secondary':'primary'} full" data-action="buy" data-id="${p.id}" ${state.owned.includes(p.id)?'disabled':''}>${state.owned.includes(p.id)?`${icon('check')} In your collection`:`Purchase ${icon('arrow')}`}</button>${!state.owned.includes(p.id)&&state.balance<p.price?`<small class="affordability">${fmt(p.price-state.balance)} more credits needed</small>`:''}</article>`).join('')}</div>`}
function confirmBuy(id){const p=products.find(p=>p.id===id);if(!p||state.owned.includes(id))return;if(state.balance<p.price){notify(`You need ${fmt(p.price-state.balance)} more credits for ${p.name}.`);return}showModal('Make it yours',`<div class="purchase-confirm">${art(p)}<span class="rarity tier-${p.tier}">${tiers[p.tier]}</span><h3>${p.name}</h3><p class="muted">${p.detail}</p><div class="confirm-row"><span>Purchase price</span>${coin(p.price)}</div><div class="confirm-row"><span>Balance after purchase</span>${coin(state.balance-p.price)}</div><button class="primary full" data-action="confirm-buy" data-id="${p.id}">Confirm purchase ${icon('check')}</button><p class="fine">Added to your inventory and automatically equipped. Virtual credits only.</p></div>`)}
function buy(id){const p=products.find(p=>p.id===id);if(!p||state.owned.includes(id)||state.balance<p.price)return;state.balance-=p.price;state.owned.push(id);state.equipped[p.type]=id;save();closeModal();render();notify(`${p.name} is yours. Added to inventory and equipped.`)}
// Read/navigation tools share the same visible state as the app and never place a bet.
if(document.modelContext?.registerTool){const lifecycle=new AbortController();window.addEventListener('pagehide',()=>lifecycle.abort(),{once:true});for(const tool of [{name:'read_monarch_collection',description:'Read the current local demo balance and owned items.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>({balance:state.balance,items:products.filter(p=>state.owned.includes(p.id)).map(p=>({id:p.id,name:p.name,equipped:state.equipped[p.type]===p.id}))})},{name:'navigate_monarch_section',description:'Open Play, Inventory, or Purchase without placing bets or buying items.',inputSchema:{type:'object',properties:{section:{type:'string',enum:['play','inventory','purchase']}},required:['section'],additionalProperties:false},annotations:{readOnlyHint:false},execute:async input=>{if(!input||!['play','inventory','purchase'].includes(input.section))throw Error('Invalid section');go(input.section);render();return {section:input.section}}}])try{Promise.resolve(document.modelContext.registerTool(tool,{signal:lifecycle.signal})).catch(()=>{})}catch(e){}}
